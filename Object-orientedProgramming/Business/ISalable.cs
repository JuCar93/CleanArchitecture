

namespace Object_orientedProgramming.Business
{
    public interface ISalable
    {
        public decimal GetPrice();  // Todas las clases que implementen la interfaz Isalable tienen que tener el método GetPrice

    }
}
